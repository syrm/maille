'use strict';

var PervasivesU = require("rescript/lib/js/pervasivesU.js");
var Webapi__Canvas__Path_Common = require("./Webapi__Canvas__Path_Common.js");

var Composite = {
  sourceOver: "source-over",
  sourceIn: "source-in",
  sourceOut: "source-out",
  sourceAtop: "source-atop",
  destinationOver: "destination-over",
  destinationIn: "destination-in",
  destinationOut: "destination-out",
  destinationAtop: "destination-atop",
  lighter: "lighter",
  copy: "copy",
  xor: "xor"
};

var LineCap = {
  butt: "butt",
  round: "round",
  square: "square"
};

var LineJoin = {
  round: "round",
  bevel: "bevel",
  miter: "miter"
};

function reifyStyle(x) {
  var isCanvasGradient = (function(x) { return x instanceof CanvasGradient; });
  var isCanvasPattern = (function(x) { return x instanceof CanvasPattern; });
  return [
          typeof x === "string" ? "String" : (
              isCanvasGradient(x) ? "Gradient" : (
                  isCanvasPattern(x) ? "Pattern" : PervasivesU.invalid_arg("Unknown canvas style kind. Known values are: String, CanvasGradient, CanvasPattern")
                )
            ),
          x
        ];
}

function fillStyle(ctx) {
  return reifyStyle(ctx.fillStyle);
}

function strokeStyle(ctx) {
  return reifyStyle(ctx.strokeStyle);
}

Webapi__Canvas__Path_Common.Impl({});

exports.Composite = Composite;
exports.LineCap = LineCap;
exports.LineJoin = LineJoin;
exports.reifyStyle = reifyStyle;
exports.fillStyle = fillStyle;
exports.strokeStyle = strokeStyle;
/*  Not a pure module */
