'use strict';

let Webapi__Dom__Node = require("./Webapi__Dom__Node.js");
let Webapi__Dom__Element = require("./Webapi__Dom__Element.js");
let Webapi__Dom__EventTarget = require("./Webapi__Dom__EventTarget.js");
let Webapi__Dom__HtmlElement = require("./Webapi__Dom__HtmlElement.js");

function Impl(T) {
  let toString = x => {
    switch (x) {
      case "Forward" :
        return "forward";
      case "Backward" :
        return "backward";
      case "None" :
        return "none";
    }
  };
  let SelectionDirection = {
    toString: toString
  };
  let setSelectionRangeWithDirection = (selectionStart, selectionEnd, selectionDirection, element) => {
    element.setSelectionRange(selectionStart, selectionEnd, toString(selectionDirection));
  };
  let toString$1 = x => {
    switch (x) {
      case "Select" :
        return "select";
      case "Start" :
        return "start";
      case "End" :
        return "end";
      case "Preserve" :
        return "preserve";
    }
  };
  let SelectionMode = {
    toString: toString$1
  };
  let setRangeTextWithinIntervalWithSelectionMode = (text, selectionStart, selectionEnd, selectionMode, element) => {
    element.setRangeText(text, selectionStart, selectionEnd, toString$1(selectionMode));
  };
  return {
    SelectionDirection: SelectionDirection,
    setSelectionRangeWithDirection: setSelectionRangeWithDirection,
    SelectionMode: SelectionMode,
    setRangeTextWithinIntervalWithSelectionMode: setRangeTextWithinIntervalWithSelectionMode
  };
}

Webapi__Dom__EventTarget.Impl({});

let include = Webapi__Dom__Node.Impl({});

let include$1 = Webapi__Dom__Element.Impl({});

let include$2 = Webapi__Dom__HtmlElement.Impl({});

function toString(x) {
  switch (x) {
    case "Forward" :
      return "forward";
    case "Backward" :
      return "backward";
    case "None" :
      return "none";
  }
}

let SelectionDirection = {
  toString: toString
};

function setSelectionRangeWithDirection(selectionStart, selectionEnd, selectionDirection, element) {
  element.setSelectionRange(selectionStart, selectionEnd, toString(selectionDirection));
}

function toString$1(x) {
  switch (x) {
    case "Select" :
      return "select";
    case "Start" :
      return "start";
    case "End" :
      return "end";
    case "Preserve" :
      return "preserve";
  }
}

let SelectionMode = {
  toString: toString$1
};

function setRangeTextWithinIntervalWithSelectionMode(text, selectionStart, selectionEnd, selectionMode, element) {
  element.setRangeText(text, selectionStart, selectionEnd, toString$1(selectionMode));
}

let nodeType = include.nodeType;

let asHtmlElement = include$1.asHtmlElement;

let ofNode = include$1.ofNode;

let insertAdjacentElement = include$1.insertAdjacentElement;

let insertAdjacentHTML = include$1.insertAdjacentHTML;

let insertAdjacentText = include$1.insertAdjacentText;

let ofElement = include$2.ofElement;

let contentEditable = include$2.contentEditable;

let setContentEditable = include$2.setContentEditable;

let dir = include$2.dir;

let setDir = include$2.setDir;

exports.Impl = Impl;
exports.nodeType = nodeType;
exports.asHtmlElement = asHtmlElement;
exports.ofNode = ofNode;
exports.insertAdjacentElement = insertAdjacentElement;
exports.insertAdjacentHTML = insertAdjacentHTML;
exports.insertAdjacentText = insertAdjacentText;
exports.ofElement = ofElement;
exports.contentEditable = contentEditable;
exports.setContentEditable = setContentEditable;
exports.dir = dir;
exports.setDir = setDir;
exports.SelectionDirection = SelectionDirection;
exports.setSelectionRangeWithDirection = setSelectionRangeWithDirection;
exports.SelectionMode = SelectionMode;
exports.setRangeTextWithinIntervalWithSelectionMode = setRangeTextWithinIntervalWithSelectionMode;
/*  Not a pure module */
