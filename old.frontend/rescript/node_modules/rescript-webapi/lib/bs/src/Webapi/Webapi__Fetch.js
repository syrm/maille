'use strict';

let Primitive_option = require("@rescript/runtime/lib/js/Primitive_option.js");

function encodeRequestMethod(x) {
  if (typeof x === "object") {
    return x._0;
  }
  switch (x) {
    case "Get" :
      return "GET";
    case "Head" :
      return "HEAD";
    case "Post" :
      return "POST";
    case "Put" :
      return "PUT";
    case "Delete" :
      return "DELETE";
    case "Connect" :
      return "CONNECT";
    case "Options" :
      return "OPTIONS";
    case "Trace" :
      return "TRACE";
    case "Patch" :
      return "PATCH";
  }
}

function encodeReferrerPolicy(x) {
  switch (x) {
    case "None" :
      return "";
    case "NoReferrer" :
      return "no-referrer";
    case "NoReferrerWhenDowngrade" :
      return "no-referrer-when-downgrade";
    case "SameOrigin" :
      return "same-origin";
    case "Origin" :
      return "origin";
    case "StrictOrigin" :
      return "strict-origin";
    case "OriginWhenCrossOrigin" :
      return "origin-when-cross-origin";
    case "StrictOriginWhenCrossOrigin" :
      return "strict-origin-when-cross-origin";
    case "UnsafeUrl" :
      return "unsafe-url";
  }
}

function encodeRequestMode(x) {
  switch (x) {
    case "Navigate" :
      return "navigate";
    case "SameOrigin" :
      return "same-origin";
    case "NoCORS" :
      return "no-cors";
    case "CORS" :
      return "cors";
  }
}

function encodeRequestCredentials(x) {
  switch (x) {
    case "Omit" :
      return "omit";
    case "SameOrigin" :
      return "same-origin";
    case "Include" :
      return "include";
  }
}

function encodeRequestCache(x) {
  switch (x) {
    case "Default" :
      return "default";
    case "NoStore" :
      return "no-store";
    case "Reload" :
      return "reload";
    case "NoCache" :
      return "no-cache";
    case "ForceCache" :
      return "force-cache";
    case "OnlyIfCached" :
      return "only-if-cached";
  }
}

function encodeRequestRedirect(x) {
  switch (x) {
    case "Follow" :
      return "follow";
    case "Error" :
      return "error";
    case "Manual" :
      return "manual";
  }
}

let HeadersInit = {};

let Headers = {};

let BodyInit = {};

function map(f, x) {
  if (x !== undefined) {
    return Primitive_option.some(f(Primitive_option.valFromOption(x)));
  }
}

function make(method_, headers, body, referrer, referrerPolicyOpt, mode, credentials, cache, redirect, integrityOpt, keepalive, signal, param) {
  let referrerPolicy = referrerPolicyOpt !== undefined ? referrerPolicyOpt : "None";
  let integrity = integrityOpt !== undefined ? integrityOpt : "";
  let tmp = {
    referrerPolicy: encodeReferrerPolicy(referrerPolicy),
    integrity: integrity
  };
  let tmp$1 = map(encodeRequestMethod, method_);
  if (tmp$1 !== undefined) {
    tmp.method = tmp$1;
  }
  if (headers !== undefined) {
    tmp.headers = Primitive_option.valFromOption(headers);
  }
  if (body !== undefined) {
    tmp.body = Primitive_option.valFromOption(body);
  }
  if (referrer !== undefined) {
    tmp.referrer = referrer;
  }
  let tmp$2 = map(encodeRequestMode, mode);
  if (tmp$2 !== undefined) {
    tmp.mode = tmp$2;
  }
  let tmp$3 = map(encodeRequestCredentials, credentials);
  if (tmp$3 !== undefined) {
    tmp.credentials = tmp$3;
  }
  let tmp$4 = map(encodeRequestCache, cache);
  if (tmp$4 !== undefined) {
    tmp.cache = tmp$4;
  }
  let tmp$5 = map(encodeRequestRedirect, redirect);
  if (tmp$5 !== undefined) {
    tmp.redirect = tmp$5;
  }
  if (keepalive !== undefined) {
    tmp.keepalive = keepalive;
  }
  if (signal !== undefined) {
    tmp.signal = Primitive_option.valFromOption(signal);
  }
  return tmp;
}

function method_(self) {
  let x = self.method;
  switch (x) {
    case "CONNECT" :
      return "Connect";
    case "DELETE" :
      return "Delete";
    case "GET" :
      return "Get";
    case "HEAD" :
      return "Head";
    case "OPTIONS" :
      return "Options";
    case "PATCH" :
      return "Patch";
    case "POST" :
      return "Post";
    case "PUT" :
      return "Put";
    case "TRACE" :
      return "Trace";
    default:
      return {
        TAG: "Other",
        _0: x
      };
  }
}

function destination(self) {
  let x = self.destination;
  switch (x) {
    case "" :
      return "None";
    case "document" :
      return "Document";
    case "embed" :
      return "Embed";
    case "font" :
      return "Font";
    case "image" :
      return "Image";
    case "manifest" :
      return "Manifest";
    case "media" :
      return "Media";
    case "object" :
      return "Object";
    case "report" :
      return "Report";
    case "script" :
      return "Script";
    case "serviceworker" :
      return "ServiceWorker";
    case "sharedworder" :
      return "SharedWorker";
    case "style" :
      return "Style";
    case "worker" :
      return "Worker";
    case "xslt" :
      return "Xslt";
    default:
      throw {
        RE_EXN_ID: "Failure",
        _1: "Unknown requestDestination: " + x,
        Error: new Error()
      };
  }
}

function referrerPolicy(self) {
  let x = self.referrerPolicy;
  switch (x) {
    case "" :
      return "None";
    case "no-referrer" :
      return "NoReferrer";
    case "no-referrer-when-downgrade" :
      return "NoReferrerWhenDowngrade";
    case "origin" :
      return "Origin";
    case "origin-when-cross-origin" :
      return "OriginWhenCrossOrigin";
    case "same-origin" :
      return "SameOrigin";
    case "strict-origin" :
      return "StrictOrigin";
    case "strict-origin-when-cross-origin" :
      return "StrictOriginWhenCrossOrigin";
    case "unsafe-url" :
      return "UnsafeUrl";
    default:
      throw {
        RE_EXN_ID: "Failure",
        _1: "Unknown referrerPolicy: " + x,
        Error: new Error()
      };
  }
}

function mode(self) {
  let x = self.mode;
  switch (x) {
    case "cors" :
      return "CORS";
    case "navigate" :
      return "Navigate";
    case "no-cors" :
      return "NoCORS";
    case "same-origin" :
      return "SameOrigin";
    default:
      throw {
        RE_EXN_ID: "Failure",
        _1: "Unknown requestMode: " + x,
        Error: new Error()
      };
  }
}

function credentials(self) {
  let x = self.credentials;
  switch (x) {
    case "include" :
      return "Include";
    case "omit" :
      return "Omit";
    case "same-origin" :
      return "SameOrigin";
    default:
      throw {
        RE_EXN_ID: "Failure",
        _1: "Unknown requestCredentials: " + x,
        Error: new Error()
      };
  }
}

function cache(self) {
  let x = self.cache;
  switch (x) {
    case "default" :
      return "Default";
    case "force-cache" :
      return "ForceCache";
    case "no-cache" :
      return "NoCache";
    case "no-store" :
      return "NoStore";
    case "only-if-cached" :
      return "OnlyIfCached";
    case "reload" :
      return "Reload";
    default:
      throw {
        RE_EXN_ID: "Failure",
        _1: "Unknown requestCache: " + x,
        Error: new Error()
      };
  }
}

function redirect(self) {
  let x = self.redirect;
  switch (x) {
    case "error" :
      return "Error";
    case "follow" :
      return "Follow";
    case "manual" :
      return "Manual";
    default:
      throw {
        RE_EXN_ID: "Failure",
        _1: "Unknown requestRedirect: " + x,
        Error: new Error()
      };
  }
}

let Request = {
  method_: method_,
  destination: destination,
  referrerPolicy: referrerPolicy,
  mode: mode,
  credentials: credentials,
  cache: cache,
  redirect: redirect
};

function make$1(status, statusText, headers, param) {
  let tmp = {};
  if (status !== undefined) {
    tmp.status = status;
  }
  if (statusText !== undefined) {
    tmp.statusText = statusText;
  }
  if (headers !== undefined) {
    tmp.headers = Primitive_option.valFromOption(headers);
  }
  return tmp;
}

let ResponseInit = {
  make: make$1
};

let Response = {};

let AbortController;

let FormData;

let Body = {};

let RequestInit = {
  make: make
};

exports.AbortController = AbortController;
exports.FormData = FormData;
exports.HeadersInit = HeadersInit;
exports.Headers = Headers;
exports.BodyInit = BodyInit;
exports.Body = Body;
exports.RequestInit = RequestInit;
exports.Request = Request;
exports.ResponseInit = ResponseInit;
exports.Response = Response;
/* No side effect */
