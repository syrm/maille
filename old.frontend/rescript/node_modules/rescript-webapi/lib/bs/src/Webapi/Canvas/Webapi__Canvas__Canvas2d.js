'use strict';

let Pervasives = require("@rescript/runtime/lib/js/Pervasives.js");
let Webapi__Canvas__Path_Common = require("./Webapi__Canvas__Path_Common.js");

let Composite = {
  sourceOver: "source-over",
  sourceIn: "source-in",
  sourceOut: "source-out",
  sourceAtop: "source-atop",
  destinationOver: "destination-over",
  destinationIn: "destination-in",
  destinationOut: "destination-out",
  destinationAtop: "destination-atop",
  lighter: "lighter",
  copy: "copy",
  xor: "xor"
};

let LineCap = {
  butt: "butt",
  round: "round",
  square: "square"
};

let LineJoin = {
  round: "round",
  bevel: "bevel",
  miter: "miter"
};

function reifyStyle(x) {
  let isCanvasGradient = (function(x) { return x instanceof CanvasGradient; });
  let isCanvasPattern = (function(x) { return x instanceof CanvasPattern; });
  return [
    typeof x === "string" ? "String" : (
        isCanvasGradient(x) ? "Gradient" : (
            isCanvasPattern(x) ? "Pattern" : Pervasives.invalid_arg("Unknown canvas style kind. Known values are: String, CanvasGradient, CanvasPattern")
          )
      ),
    x
  ];
}

function fillStyle(ctx) {
  return reifyStyle(ctx.fillStyle);
}

function strokeStyle(ctx) {
  return reifyStyle(ctx.strokeStyle);
}

Webapi__Canvas__Path_Common.Impl({});

exports.Composite = Composite;
exports.LineCap = LineCap;
exports.LineJoin = LineJoin;
exports.reifyStyle = reifyStyle;
exports.fillStyle = fillStyle;
exports.strokeStyle = strokeStyle;
/*  Not a pure module */
