'use strict';


function classify(t) {
  if (typeof t === "string") {
    return {
      NAME: "String",
      VAL: t
    };
  } else {
    return {
      NAME: "File",
      VAL: t
    };
  }
}

let EntryValue = {
  classify: classify
};

exports.EntryValue = EntryValue;
/* No side effect */
