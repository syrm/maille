'use strict';

let Primitive_option = require("@rescript/runtime/lib/js/Primitive_option.js");

let testRadioNodeList = (function(el) {
    if ((el.constructor.name != null && el.constructor.name === "RadioNodeList")
      || /^\[object RadioNodeList\]$/.test(el.constructor.toString())) {
      return el;
    }
  });

function namedItem(t, name) {
  let value = t.namedItem(name);
  if (value == null) {
    return;
  }
  let radioNodeList = testRadioNodeList(value);
  if (radioNodeList !== undefined) {
    return {
      TAG: "RadioNodeList",
      _0: Primitive_option.valFromOption(radioNodeList)
    };
  }
  let match = value.tagName.toUpperCase();
  switch (match) {
    case "BUTTON" :
      return {
        TAG: "Button",
        _0: value
      };
    case "FIELDSET" :
      return {
        TAG: "FieldSet",
        _0: value
      };
    case "INPUT" :
      return {
        TAG: "Input",
        _0: value
      };
    case "OBJECT" :
      return {
        TAG: "Object",
        _0: value
      };
    case "OUTPUT" :
      return {
        TAG: "Output",
        _0: value
      };
    case "SELECT" :
      return {
        TAG: "Select",
        _0: value
      };
    case "TEXTAREA" :
      return {
        TAG: "TextArea",
        _0: value
      };
    default:
      return;
  }
}

exports.namedItem = namedItem;
/* No side effect */
