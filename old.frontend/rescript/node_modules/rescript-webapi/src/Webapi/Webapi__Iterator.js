'use strict';


function forEach(f, t) {
  while (true) {
    let item = t.next();
    let match = item.done;
    let match$1 = item.value;
    if (match !== undefined && match) {
      if (!(match$1 == null)) {
        return f(match$1);
      } else {
        return;
      }
    }
    if (match$1 == null) {
      continue;
    }
    f(match$1);
    continue;
  };
}

exports.forEach = forEach;
/* No side effect */
